# Enable C++ exceptions and RTTI (required for standard C++ library features)
CONFIG_COMPILER_CXX_EXCEPTIONS=y
CONFIG_COMPILER_CXX_RTTI=y

# Give app_main a generous stack
CONFIG_ESP_MAIN_TASK_STACK_SIZE=4096
